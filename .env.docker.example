# Copia este archivo a .env (docker compose lo carga automáticamente)

# --- Postgres ---
POSTGRES_USER=melgar
POSTGRES_PASSWORD=cambia_este_password
POSTGRES_DB=melgarbot

# --- n8n ---
N8N_HOST=localhost
WEBHOOK_URL=http://localhost:5678/

# --- pgadmin ---
PGADMIN_EMAIL=admin@melgar.dev
PGADMIN_PASSWORD=admin

# --- Admin (Next.js) ---
JWT_SECRET=genera-con-openssl-rand-hex-32
GEMINI_API_KEY=AIza...
